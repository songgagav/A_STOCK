"""一次性: 登记「形态 ④b 边界腐烂」与「识别条件必须覆盖所有格式」(2026-09-26 用户要求)。

同时把上批登记的 `BUG-GUARD-SLICE-BOUNDARY-ROT` 标注为**已升格为正式形态 ④b**,
避免同一件事在登记册里出现两个"权威"条目(本仓教训: 同一件事写两处, 迟早只剩一处是对的)。
"""
from __future__ import annotations

import json
import os

FP = os.path.join("ops", "acceptance_status.json")
d = json.load(open(FP, encoding="utf-8"))
items = d["items"]
have = {it["id"] for it in items}

NEW = [
    {
        "id": "ROADMAP-DISC2-FORM4B-BOUNDARY-ROT",
        "level": "P1",
        "title": "DISC-2 补形态 ④b「边界腐烂」: 按标识切片, 而标识已被改名 ⇒ find 返 -1 ⇒ 切片跑到文末",
        "status": "fixed",
        "detail": (
            "用户 2026-09-26 要求补 ④b「边界腐烂」, 与 ④ **并列**"
            "(不是第 10 种形态)。修法: 共享常量 + 「边界恰好一次」闸门。\n\n"
            "## 与 ④ 的分工\n\n"
            "④ 坏在**规格**(测试在验证一条错的规定); ④b 坏在**定位**"
            "(测试在验证一片错误的区域)。\n\n"
            "## 根因: `-1` 在切片里是合法索引\n\n"
            "```python\n"
            "j = src.find(\"### 四种\", i)     # 该标题已被改名 ⇒ 返回 -1\n"
            "block = src[i:j]                # src[i:-1] ⇒ 取到**几乎整篇**\n"
            "```\n\n"
            "`find` 用 `-1` 表示「没找到」, 而 `-1` 在切片里是个**合法的负索引**\n"
            "⇒ 「找不到边界」被静默翻译成「取到文末」。Python 不报错。\n\n"
            "## 实例(2026-09-26)\n\n"
            "DISC-2 详细章节标题由 `### 四种\"测试看起来正常\"…` 改为\n"
            "`### 详细判据: 四种… + 三种…`, 而**三个用例仍按 `### 四种` 切片**:\n\n"
            "| 受影响断言 | 方向 | 后果 |\n|---|---|---|\n"
            "| 「每列必须给出可执行的检查方法」 | 正向 | 变成在**全篇**找形态行 ⇒ 比设计**更松** |\n"
            "| 「索引里不该有多列形态」 | **反向** | 切片被详细章节喂饱 ⇒ **恒真** |\n\n"
            "**失效方向几乎总是「断言变弱」** ⇒ 报告更绿, 无任何信号。故列为 🔴 高。\n\n"
            "## 修法(两道闸门, 缺一不可)\n\n"
            "1. **标识集中成常量** —— `_DETAIL_HEADING`, 三个用例共享, 不许多处各写一份;\n"
            "2. **加一条「边界确实存在」的闸门**:\n"
            "   · 恰好一行以该标题开头;\n"
            "   · `0 < i < j` 成立(而非 -1);\n"
            "   · **索引块占幅 < 半篇** —— 这一步是关键: 前两条只保证标识**存在**,\n"
            "     不保证切片**合理**; 而本题失效形态恰是「边界不存在 ⇒ 切片异常巨大」。\n"
            "     占幅断言是唯一能自动发现「切歪了」的判据。\n\n"
            "## 同族先例\n\n"
            "本仓已修的「告警规则从未被加载」是同一形态的**运行时版本**\n"
            "(相对路径导致读的是另一份文件, 而文件存在、也不报错)。\n"
            "④b 是它的**测试版本**: 两份都「存在且合法」, 但引用的那个不是被测的那个。"
        ),
        "evidence": (
            "docs/disciplines.md: 失效形态索引加 ④b 行(🔴 高)、详细判据对照表加 ④b 行、"
            "新增 `#### ④b 边界腐烂` 小节(根因 -1 合法索引 / 受影响断言表 / 两道闸门 / 通用判据 / "
            "同族先例); tests/test_discipline_doc.py: `_FORMS` 扩为 9 项含 ④b, "
            "`test_index_marks_the_hard_to_find_forms_as_high_priority` 纳入 ④b, "
            "`test_the_detail_heading_did_not_rot` 改为**行首锚定**并加占幅断言; "
            "实测修复前 `find(\"### 四种\")` 返回 -1 已确认。"
        ),
        "next": (
            "维持。**通用要求**: 凡按标识(标题/键名/标记串/分隔符)切片的代码, "
            "都要问「这个标识还在吗? 它唯一吗?」—— 两个问题的答案必须是**运行时断言**, "
            "不能是「我记得它在」。\n"
            "本条与 `BUG-GUARD-SLICE-BOUNDARY-ROT` 是同一件事(后者为发现记录, 已标注升格); "
            "后续以本条为准。"
        ),
    },
    {
        "id": "ROADMAP-GUARD-RECOGNITION-MUST-COVER-ALL-FORMATS",
        "level": "P1",
        "title": "固化「守卫的识别条件必须覆盖该节所有可能格式」: 两天内踩了三次同一个坑",
        "status": "fixed",
        "detail": (
            "用户 2026-09-26 要求写入纪律。\n\n"
            "## 判据\n\n"
            "> 写「识别/排除条件」时先问: 「**这一节可能出现哪些格式? 我覆盖了几种?**」\n"
            "> 只覆盖**当时**那一种 ⇒ 结构一演进, 守卫就静默失效或反向误报。\n\n"
            "## 三次实测(每一条都是真的)\n\n"
            "| # | 识别条件写的 | 结构怎么变的 | 后果 |\n|---|---|---|---|\n"
            "| 1 | 按**源码文本**找 `yaml.safe_load` | 守卫自己的 docstring 里也有这串字 | **报了自己**(良性: 报错可见) |\n"
            "| 2 | 「用没用 PyYAML」看函数体是否含 `_load_rules(` | 另一守卫的**测试样本串**里也含它 | **误报样本**(良性) |\n"
            "| 3 | 叙述区 = **`>` 开头的块** | 病历从 `>` 块**搬进正文** `### ⑦` 小节 | **反向误报**: 守卫去报**病历** |\n\n"
            "三次根因**同一个**: 把「识别条件」写成了**当时文档的偶然形状**"
            "(某个前缀、某个函数名、某个位置), 而不是「这一节在语义上是什么」。\n\n"
            "## 修法: 用语义特征, 不只用位置/字面量\n\n"
            "| ❌ 位置/字面量(会腐烂) | ✅ 语义特征(较耐久) | 仍需配覆盖检查 |\n|---|---|---|\n"
            "| 「`>` 开头的行」 | 「引用错误的那几行」 | 二者取并集 + 断言并集非空 |\n"
            "| `find(\"### 四种\")` | 行首锚定 + 「恰好一次」 | 见 ④b |\n"
            "| 函数体含某串 | **AST 调用节点**而非文本 | 见 ① 的 docstring 实例 |\n\n"
            "## 落地要求(两条, 缺一不可)\n\n"
            "1. **枚举该节的可能格式** —— 写识别条件时在注释里**列出**考虑过的格式"
            "(`>` 块 / 正文小节 / 表格行 / 多行字符串), 并说明为什么覆盖或不覆盖;\n"
            "2. **加一条「识别区非空」的反向验证** —— 本次即\n"
            "`test_the_narrative_regions_are_detected`(要求至少一行叙述真的引用了「误报」,\n"
            "且 ⑦ 小节与所有 `>` 行都被标进叙述区)。\n"
            "**没有这条, 识别条件可能悄悄变成「识别一切」或「识别不到任何东西」** ——\n"
            "前者让主断言变空, 后者让它恒真。**两种都是「看起来在检查」。**\n\n"
            "## 与 ④b 的分工\n\n"
            "④b 管**切片边界**(标识还在吗); 本条管**识别条件**(格式覆盖全吗)。\n"
            "二者常同时出现: 本次三个用例既按已改名的标题切片(④b), 又只认 `>` 块这一种格式(本条)。"
        ),
        "evidence": (
            "docs/disciplines.md: 守卫设计原则下新增"
            "`#### 守卫的设计原则之二: 识别条件必须覆盖该节的所有可能格式`, "
            "含三次实测表 / 语义特征对照表 / 两条落地要求 / 与 ④b 的分工; "
            "「可自动化程度」段同步补 ④b; "
            "tests/test_discipline_doc.py 新增 `TestGuardRecognitionMustCoverAllFormats`"
            "(原则存在 / 三次都列出 / 要求识别区非空断言 / 与 ④b 分工); "
            "另: 该纪律在实现过程中**当场又触发一次** —— 新守卫按 `src.find(\"### ⑦\")` 定位小节, "
            "命中了块引用里的一个标题(该块恰好含 ⑦ 字样) ⇒ 叙述区被算到几千行; "
            "已改为「行首锚定 + 排除引用行」并加**占幅 < 半篇**断言, 详见用例 docstring"
        ),
        "next": (
            "维持。**通用要求**: 凡守卫带「识别/排除条件」的, 必须\n"
            "① 在注释里**枚举**考虑过的格式;\n"
            "② 配一条「识别区非空**且不过宽**」的反向验证。\n"
            "「不过宽」这一半同样重要 —— 本次只写「非空」是不够的, "
            "因为 bug 是**识别过头**(把正文主张也划进叙述区)。"
        ),
    },
]

added = 0
for e in NEW:
    if e["id"] in have:
        print("skip (already present):", e["id"])
        continue
    items.append(e)
    added += 1

# 把先前的发现记录标注为「已升格为正式形态 ④b」, 避免两个"权威"条目并存
marked = 0
for it in items:
    if it.get("id") == "BUG-GUARD-SLICE-BOUNDARY-ROT":
        it["title"] = (
            "【已升格为正式形态 ④b, 见 ROADMAP-DISC2-FORM4B-BOUNDARY-ROT】"
            "三个守卫按**已改名**的小节标题切片 ⇒ find 返回 -1 ⇒ 切片跑到文末 ⇒ 断言恒真"
        )
        it["next"] = (
            "**本条已升格为 DISC-2 正式形态 ④b** —— 判据/修法/通用要求以 "
            "`ROADMAP-DISC2-FORM4B-BOUNDARY-ROT` 为准, 本条保留为**发现记录**。"
            "(同一件事不在登记册里放两个「权威」条目: 写两处, 迟早只剩一处是对的。)"
        )
        marked += 1

assert marked == 1, f"expected 1 cross-reference update, got {marked}"

d["generated_at"] = "2026-09-26"
with open(FP, "w", encoding="utf-8", newline="\n") as fh:
    json.dump(d, fh, ensure_ascii=False, indent=2)
    fh.write("\n")

print("added:", added, "| cross-ref updated:", marked, "| total items:", len(items))
