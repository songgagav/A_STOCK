"""一次性: 登记 2026-09-26 这批「用户要求的 DISC-2 形态补充」(登记册为权威记录)。

用户清单:
  □ DISC-2 补充形态 ⑦「证据无判别力」
  □ DISC-2 补充形态 ③b「前置是环境事实」
  □ DISC-2 补充「守卫的负样本必须能触发守卫」

顺带登记在实现过程中**挖出的两个真问题**(属于同一批工作, 不另开条目):
  · 守卫切片边界腐烂(三个用例按已改名的标题切片 ⇒ 断言恒真)
  · 叙述区识别失效(⑦ 升格后病历搬进正文, 只认 `>` 的排除条件立刻误报)
"""
from __future__ import annotations

import json
import os

FP = os.path.join("ops", "acceptance_status.json")
d = json.load(open(FP, encoding="utf-8"))
items = d["items"]
have = {it["id"] for it in items}

NEW = [
    {
        "id": "ROADMAP-DISC2-FORM7-NO-DISCRIMINATING-POWER",
        "level": "P1",
        "title": "DISC-2 补第 ⑦ 种形态「证据无判别力」: 证据为真但对两个互斥假设都成立",
        "status": "fixed",
        "detail": (
            "用户 2026-09-26 要求把「证据无判别力」由**共同原则下的一条判据**"
            "升格为 DISC-2 的**正式形态 ⑦**, 并给出三段式结构。\n\n"
            "## 判据\n\n"
            "> 拿到一条证据时先问: 「**如果反过来才是真的, 这条证据会不一样吗?**」\n"
            "> · 会不一样 ⇒ 有判别力, 可作为确认;\n"
            "> · **不会不一样 ⇒ 只是「与假设相容」, 不构成确认** ⇒ 继续找能区分两者的观测。\n\n"
            "## 实例(2026-09-26, 真实事故)\n\n"
            "我把告警 `DrlEnvMissing` 登记成「已知结构性误报」, 依据是 09-24 的\n"
            "`drl_train_heartbeat.json` = `{phase: done, ok: true, 800 timesteps}`。\n"
            "**这条证据是真的**, 但它对两个互斥假设**都成立**:\n\n"
            "| 假设 | 训练成功? | 探针报缺依赖? | 与心跳一致? |\n|---|---|---|---|\n"
            "| (a) 探针探的是**另一个**解释器 | ✅ | ✅ | ✅ |\n"
            "| (b) 探针探的**就是**训练那个解释器 | ✅ | ❌ | ✅ |\n\n"
            "**判别观测**是「`PY` 从哪来」, 而不是「训练成不成功」:\n"
            "`src/daemon.py:35-40` 是 `PY = sys.executable`(可被 `TRAE_PYTHON` 覆盖)的"
            "**单一来源**, metrics_server 与 run_daily **共用它** ⇒ 所有子进程天然同源;\n"
            "`scripts/start_daemon.ps1` 默认用 `.venv310`, 而 `P0-DRLDEP` 已于 09-20 修复。\n"
            "⇒ 结论是 (b), 该规则**从未真正故障**。\n\n"
            "## 后果(与其它形态最不同)\n\n"
            "**它污染的不是过程, 是结论** —— 错误判断被写进注释/登记册,\n"
            "于是**后来的人也照错**。一个标着「已知误报」的**真**告警**比没有告警更坏**:\n"
            "没有告警时人会去找; 有告警但被文字解释掉时, 人以为已经处理过了。\n"
            "**这是 ⑥ 的反向形态: 告警在, 但被文字解释掉了。**\n\n"
            "## 与其它形态的边界(已写进文档)\n\n"
            "- 不是 ①: 证据是真的, 断言也没写错;\n"
            "- 不是 ②: 证据确实被执行、确实为真;\n"
            "- 不是 ④: 规格没错, 错的是**从证据到结论的那一跳**;\n"
            "- 不是 ⑤: 信息没有在中间层丢失, 它完整地到了我这里;\n"
            "- 不是 ⑥: 这里没有发生降级, 一切「正常」。\n\n"
            "⑤ 的输入是**错的**, ⑦ 的输入是**对的** ⇒ 后者更难发现, 因为没有任何东西"
            "看起来不对。⑤ 问「这条错误信息有没有独立数字能证伪它」(排查方向);\n"
            "⑦ 问「这条**正确**的证据能不能区分假设」(确认效力)。"
        ),
        "evidence": (
            "docs/disciplines.md 新增 `### ⑦ 证据无判别力` 小节(判据 / 实例 / 后果 / "
            "边界 / 与 ⑤ 的分工); 失效形态索引由 6 行扩到 8 行, ⑦ 与 ④⑤⑥ 同级为🔴高; "
            "「详细判据」小标题由「四种 + 两种」改为「四种 + 三种」; "
            "章首共同原则下原判据压缩为**指向 ⑦ 的索引**(避免同一件事写两处); "
            "tests/test_discipline_doc.py 的 `_FORMS` 扩为 8 项、"
            "`test_index_marks_four_forms_as_high_priority` 纳入 ⑦、"
            "新增 `test_form3b_and_form7_are_both_in_the_index`(各恰好一行)与"
            "反向断言「索引里不得出现正文未写的编号(如 ⑧)」; "
            "实测: 默认 2456 passed / 50 skipped / 0 failed; "
            "桥接 PyYAML 后 2478 passed / 28 skipped / 0 failed"
        ),
        "next": (
            "维持。⑦ **无法自动化** —— 它问的是「这条证据的逻辑效力」, "
            "机器判不了「两个假设是否互斥」。故只能在**写结论时**(尤其是要写进"
            "注释/登记册/文档等「留给后人看」的地方)人工过一次那个问句。"
            "本条与 `P1-DRLENVMISSING-KNOWN-STRUCTURAL` 的更正配套。"
        ),
    },
    {
        "id": "ROADMAP-DISC2-FORM3B-ENV-FACT-PRECONDITION",
        "level": "P1",
        "title": "DISC-2 补形态 ③b「前置是环境事实」: 前置依赖 gitignored 状态, 被正常运维操作改掉",
        "status": "fixed",
        "detail": (
            "用户 2026-09-26 要求补 ③b「前置是环境事实」, 与 ③ **并列**"
            "(不是第 9 种形态)。\n\n"
            "## 与 ③ 的分工\n\n"
            "| | ③ 前置状态没造出来 | **③b 前置是环境事实** |\n|---|---|---|\n"
            "| 前置怎么没的 | 测试**自己没造**(漏了那一步) | 测试**没造过** —— 它**假设**环境本来就是这样 |\n"
            "| 谁改变了它 | 没人; 它从头到尾不存在 | **外部**, 而且是**正常操作**(如按运维预案建了个文件) |\n"
            "| 表现 | 用例在验证错误的场景 | 用例**红了**, 但代码**完全正确** |\n"
            "| 修法 | **先断言前置条件** | 把前置从**环境事实**改成**显式输入** |\n\n"
            "## 实例(2026-09-25)\n\n"
            "`TestDefaultOff::test_disabled_by_default` 断言 `BT.is_enabled({}) is False`,\n"
            "隐含前提是「这台机器上没有开关文件」。而 `data/backfill_switch.json` 在 `data/` 下,\n"
            "**`data/` 是 gitignored 的**。当天我**按自己写的 09-28 预案**把它建成\n"
            "`{\"enabled\": true}` ⇒ `{}.get(ENABLED_ENV)` 返回 `None` ⇒ 代码按设计回落到读开关文件\n"
            "⇒ 返回 `True`。**实测现象: 2431 passed / 1 failed, 而代码一行都没改。**\n\n"
            "**这不是回归, 是「按预案正常操作」把一个隐性前置条件打掉了。**\n"
            "代码行为完全正确; 错的是断言把「没有配置」当成了**环境事实**而非**显式输入**。\n\n"
            "## 危害为什么是「高」\n\n"
            "不是「测试写松了」, 而是**守卫会在正确操作下变红**;\n"
            "且失败信息指向「默认值不对」, 诱导人去改**代码** —— 而代码是对的。\n"
            "真正的代价: 若不追根因, 最可能的处理是**放宽断言**(等于删掉这条守卫),\n"
            "正是「守卫的设计原则」描述的退化过程。\n\n"
            "## 修法与通用判据\n\n"
            "修法: 传入一个确定不存在的 `switch_fp=tmp_path/no_such_switch.json`;\n"
            "文件路径本身的行为由 `TestSwitchFileBecauseEnvAloneIsNotEnough` 用真实 tmp 文件覆盖。\n\n"
            "> **可操作判据**: 问「**这条断言依赖的任何东西, 是否都在版本控制里?**」\n"
            "> 不在(开关文件、`data/`、`logs/`、环境变量、网络、壁钟时间、别的机器) ⇒\n"
            "> 它必须被**显式注入**, 否则这条守卫的绿是**机器给的, 不是代码给的**。\n\n"
            "**附注(为什么这类问题会越来越多)**: 本项目的运维预案本身就在写 `data/` 下的文件\n"
            "(开关、登记、溯源)。这是好事, 但意味着「测试的隐含前提」与「运维的显式动作」\n"
            "**共用同一个目录** —— 二者的冲突只会随运维成熟而**增加**, 不会减少。"
        ),
        "evidence": (
            "docs/disciplines.md 新增 `#### ③b 前置是环境事实` 小节(与 ③ 对照表 / 实例 / "
            "危害表 / 修法 / 通用判据 / 附注); 详细判据对照表加 ③b 行; 失效形态索引加 ③b 行"
            "(🟡 中); 章首原叙述块压缩为指向 ③b 的索引; "
            "`test_distinguishes_it_from_form3` 改为在 ③b 小节内取证并要求写明「并列」; "
            "新增 `docs` 侧断言: ③b 一节必须点名 backfill_switch.json / "
            "test_disabled_by_default / 2431 这三个**真实**事实"
        ),
        "next": (
            "维持。③b **容易静态化**(断言「不依赖 gitignored 路径」即可静态查),\n"
            "但本次未做全仓扫描 —— 若将来再出现同类(断言依赖 `data/` `logs/` 下的存在性),\n"
            "可考虑加一条 AST 守卫: 凡断言里出现 `data/` 或 `logs/` 字面量路径的用例, "
            "必须显式注入该路径。"
        ),
    },
    {
        "id": "ROADMAP-GUARD-NEGATIVE-SAMPLE-MUST-FIRE",
        "level": "P1",
        "title": "DISC-2 补「守卫的负样本必须能触发守卫」: 空守卫显示为全绿",
        "status": "fixed",
        "detail": (
            "用户 2026-09-26 要求单列。这不是测试技巧, 而是**存在性证明**。\n\n"
            "## 判据\n\n"
            "> 「**把被判据禁止的东西造出来, 这条守卫会红吗?**」\n"
            "> 不会红 ⇒ 它没有在守任何东西。\n\n"
            "做法: 给守卫配一个**确定违规**的样本, 并**断言它真的被抓到**。\n"
            "**注意样本本身也可能被判据过滤掉** —— 故不能只断言「扫描通过」"
            "(空守卫同样满足那一条)。\n\n"
            "## 实例: 一条空守卫的完整病历(三版返工)\n\n"
            "写 `TestNoTestCallsAnUndefinedHelper`(查「用例调用了不存在的 helper」)时,\n"
            "我**连写了三版**, 前两版都不能用:\n\n"
            "| 版本 | 做法 | 结果 |\n|---|---|---|\n"
            "| v1 | 收集 `def`/参数名再比对 | **大量假阳性**: 内置 `__import__`、函数内局部定义的类都被报成「未定义」 |\n"
            "| v2 | `symtable` 遍历子表 | 仍有假阳性: `seen`/`called` 这类**在别的作用域里赋值**的名字被误报 |\n"
            "| v3 | 只看 `Call` + 「任何作用域都没绑过此名」 + 跳过首字母小写 | **0 假阳性**(全仓 90 个测试文件), 且能抓到原缺陷 |\n\n"
            "**v2 为什么也错**: 我只看了「本函数的符号表」, 而 `seen` 是在**别的函数**里赋的值\n"
            "⇒ 判据比语言语义**更窄** ⇒ 误报。**会误报的守卫等于没有** —— 它会被"
            "「顺手」放宽或删掉。\n\n"
            "## 最贵的一课(v1 的自我打脸)\n\n"
            "v1 的反向样本我用的是 `_LoadRules()`, 而过滤器写成\n"
            "`if not nm[:1].isupper(): continue` —— `\"_\"[:1].isupper()` 是 **False**\n"
            "⇒ **连反向样本自己都被跳过**。于是「负样本没报警」被我读成「负样本通过」。\n\n"
            "> **一个连负样本都抓不到的守卫, 会显示为全绿。**\n\n"
            "故「负样本必须被抓到」这条断言**不是装饰**, 是这类守卫的**唯一存在性证明**。"
        ),
        "evidence": (
            "docs/disciplines.md 在「空守卫」病历后追加「推广到…」段, 明确写为"
            "**存在性证明**并给出判据与做法; "
            "tests/test_discipline_doc.py 的 `TestNoTestCallsAnUndefinedHelper` 含"
            "`test_negative_samples_are_actually_caught`(三个必须被抓的样本: "
            "`_load_helper()` / `_SomeHelper()` / `UnknownThing()`)与"
            "`test_positive_samples_are_not_flagged`(已定义 helper / fixture 注入 / "
            "局部定义 / import 别名四类不得误报); "
            "实测该守卫第三版全仓 0 假阳性"
        ),
        "next": (
            "维持。**推广**: 凡「静态扫描」类守卫(按源码文本/AST 判断语义的),\n"
            "都应带一个「它必须报警」的样本 —— 建议后续新增此类守卫时一并写, "
            "并在 code review 时专门看这一条。"
        ),
    },
    {
        "id": "BUG-GUARD-SLICE-BOUNDARY-ROT",
        "level": "P2",
        "title": "三个守卫按**已改名**的小节标题切片 ⇒ find 返回 -1 ⇒ 切片跑到文末 ⇒ 断言恒真",
        "status": "fixed",
        "detail": (
            "## 怎么发现的(这正是 ③b 的近亲)\n\n"
            "2026-09-26 把 DISC-2 的详细章节标题由 `### 四种\"测试看起来正常\"…`\n"
            "改为 `### 详细判据: 四种… + 三种…` 之际, 顺手核对三个切片用例, 发现:\n\n"
            "```python\n"
            "j = src.find(\"### 四种\", i)      # 标题已改名 ⇒ 返回 -1\n"
            "block = src[i:j]                 # src[i:-1] ⇒ 取到**几乎整篇**\n"
            "```\n\n"
            "⇒ `test_index_names_a_concrete_check_per_form` 实际在**全篇**里找形态行行\n"
            "(索引表之外也能命中);\n"
            "⇒ 更严重的是「索引里不该有多列形态」这类**反向**断言变成**恒真** ——\n"
            "因为详细章节的内容把切片喂饱了。\n\n"
            "**形态归属**: 与 ③b 同族 —— 用例依赖了一个**被外部改掉的字面量**。\n"
            "区别是 ③b 依赖的是 gitignored **文件**, 本条依赖的是**文档标题文本**。\n\n"
            "## 修法\n\n"
            "1. 三个用例改为共享类常量 `_DETAIL_HEADING`, 不各写一份字面量;\n"
            "2. 新增 `test_the_detail_heading_did_not_rot`:\n"
            "   · 该标题在全文**恰好出现一次**;\n"
            "   · `0 < i < j` 成立;\n"
            "   · **索引块不得占全篇一半以上**(若切片边界失效, 索引块会异常巨大)。\n\n"
            "**为什么加第 3 条**: 前两条只保证边界存在, 不保证**切片合理**;\n"
            "而本次的失效正是「边界不存在 ⇒ 切片异常」。占幅断言是能自动发现这一点的闸门。"
        ),
        "evidence": (
            "tests/test_discipline_doc.py: `TestDisc2FormIndex._DETAIL_HEADING` 常量 + "
            "`test_the_detail_heading_did_not_rot`(恰好一次 / 先后次序 / 占幅 < 半篇); "
            "三个切片用例统一改用该常量; 修复前 `find(\"### 四种\")` 返回 -1 已实测确认; "
            "全量测试默认 2456 passed / 桥接后 2478 passed, 均 0 failed"
        ),
        "next": (
            "维持。「按文档小节标题切片」这类用例的通用要求:\n"
            "**边界字面量必须集中成常量, 且必须有一条断言它在文中恰好出现一次** —— \n"
            "否则标题一改名, 所有基于它的切片都会静默退化(而且方向多半是「断言变弱」,\n"
            "即报告更绿, 正是 DISC-2 要防的方向)。"
        ),
    },
    {
        "id": "BUG-GUARD-NARRATIVE-REGION-ROT",
        "level": "P2",
        "title": "叙述区只认 `>` 块 ⇒ 病历搬进正文后, 「不得标成误报」的守卫立刻误报",
        "status": "fixed",
        "detail": (
            "## 现象\n\n"
            "`test_does_not_call_drlenvmissing_a_false_positive` 的设计是:\n"
            "**禁止主张「DrlEnvMissing 是误报」, 但允许病历里原样引用这个错误**。\n"
            "为此它把「`>` 开头的块」判为叙述区并跳过。\n\n"
            "2026-09-26 用户要求把 ⑦ 升格为正式形态后, ⑦ 的病历从 `>` 块**搬进正文**\n"
            "`### ⑦` 小节 ⇒ 原来的排除条件不再覆盖它 ⇒ 守卫**报了我自己的病历**。\n\n"
            "## 为什么这不是「守卫太严」而是一条真教训\n\n"
            "它连踩**三次**假阳性, 每次都是同一类原因(排除条件与文档结构脱节):\n\n"
            "1. 第一版扫全文 ⇒ 报病历;\n"
            "2. 第二版只排除 `>` 块 ⇒ 汇总表里那行更正文案(「不是误报! 曾于 09-25 被错标」)被报;\n"
            "3. 第三版(本次) ⑦ 升格 ⇒ 病历搬进正文 ⇒ 又报。\n\n"
            "**「一个会误报的守卫等于没有」** —— 它会被顺手放宽。故第三次不是改文档去迁就守卫,\n"
            "而是把「叙述区」的定义改成**与结构无关的判据**。\n\n"
            "## 修法\n\n"
            "`_is_narrative()`: 叙述区 = `>` 块 **或** `### ⑦` 小节(病历所在),\n"
            "并新增 `test_the_narrative_regions_are_detected`:\n"
            "· 至少有一行叙述真的引用了「误报」(否则排除条件是**空转**);\n"
            "· ⑦ 小节必须被标进叙述区;\n"
            "· 所有 `>` 行必须被标进叙述区。\n\n"
            "**为什么必须加这条反向验证**: 排除条件写宽(把一切都当叙述)会让主守卫变成"
            "**空守卫** —— 而那正是同日刚立的「负样本必须能触发守卫」要防的东西。"
        ),
        "evidence": (
            "tests/test_discipline_doc.py: `_is_narrative()` 静态方法 + "
            "`test_the_narrative_regions_are_detected`(反向验证叙述区非空且覆盖两处); "
            "`test_the_guard_is_not_vacuous` 补 `narrative=True` 分支; "
            "实测三次假阳性过程已写入用例 docstring; 全量 0 failed"
        ),
        "next": (
            "维持。通用要求: 凡守卫带「允许在病历里引用错误」这类排除条件的,\n"
            "**排除条件必须有一条反向验证**(证明它命中了真实存在的叙述区),\n"
            "否则排除条件可能悄悄变成「排除一切」⇒ 主断言恒真。"
        ),
    },
]

added = 0
for e in NEW:
    if e["id"] in have:
        print("skip (already present):", e["id"])
        continue
    items.append(e)
    added += 1

d["generated_at"] = "2026-09-26"
with open(FP, "w", encoding="utf-8", newline="\n") as fh:
    json.dump(d, fh, ensure_ascii=False, indent=2)
    fh.write("\n")

print("added:", added, "| total items:", len(items))
