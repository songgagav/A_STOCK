# ============================================================
# 开发 / 测试 / 观测栈依赖 (在 requirements_314.txt 之上)
# 本地参考环境: .venv314 (Python 3.14)
# 安装: .venv314\Scripts\python.exe -m pip install -r requirements-dev.txt
# ============================================================
# --- 测试 ---
pytest>=8.0
duckdb>=1.0            # db.py 顶层依赖; 测试环境必备
polars>=1.0            # vnpy_backtest.py 顶层依赖; 无它则该模块无法被测试导入
# --- DRL 训练/环境 (torch 建议用 CPU wheel: --index-url https://download.pytorch.org/whl/cpu) ---
torch>=2.6
stable-baselines3>=2.3
gymnasium>=1.0
# --- 观测栈 (Celery/Redis/Prometheus/Grafana/Flower) ---
celery>=5.4
redis>=4.6,<5          # Windows 原生 redis 5.0 仅支持 RESP2, 需 redis-py 4.x
prometheus_client>=0.20
flower>=2.0
psutil>=6.0
requests>=2.32
# --- 主数据源 h5i 的依赖（**仅 CPython 3.10 可装**）---
# h5i_db 的原生扩展只支持 CPython 3.10（见 README 第 171 行），故**不能**作为本文件的
# 生效依赖: 本文件面向 .venv314(Python 3.14)，写进去会让整个文件装不上。
# 但自 m4(2026-09-05) 起 h5i 是**主数据源**，h5i-db 是必需依赖 —— 此前在
# requirements_314.txt / requirements-dev.txt 中**均无声明**（登记册 P0-DRLDEP）。
# 需要读写主源数据（含 DRL 的 _load_factor_state / _load_target_plan 取数）时，
# 请在 **3.10 解释器**里单独安装:
#     <python3.10> -m pip install "h5i-db>=0.1.6"
# 注意: DRL 训练还需要 torch/gymnasium/stable-baselines3，且必须与 h5i_db 在**同一个**
# 解释器里 —— 这就是 P0-DRLDEP 的根因（当前无任何解释器同时具备两者）。
