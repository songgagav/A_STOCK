# stockdb 厂商数据源: 已知状态与定位

> 建档 2026-09-22。本文回答三个问题: **三库为什么不补**、**厂商是否停更**、
> **数据源该以谁为主**。所有结论都附实测证据, 可直接复核。

---

## 1. 结论速览

| 问题 | 结论 |
|---|---|
| 三个本地 leveldb 库停在 2026-09-03, 要不要补? | **不补**。详见 §2 |
| 厂商是否已停更? | **未确认, 但证据指向"需人核实授权/服务"**。详见 §3 |
| 评估链路依赖三库吗? | **不依赖**。评估只读 h5i。详见 §2 |
| 主数据源定位 | 暂**保留 stockdb 为主源**(引擎当前可用), akshare 作**已验证的口径备份**。详见 §4 |

---

## 2. 三个本地库: 为什么不补

### 实测状态

| 库 | 大小 | 文件数 | 最后写入 |
|---|---|---|---|
| `stockdb/data` | 21.3 GB | 672 | **2026-09-03 01:03:13** |
| `stockdb/data1` | 0.4 GB | 21 | **2026-09-03 01:03:13** |
| `stockdb/mydb` | 371 B | 6 | **2026-09-03 01:03:13** |

### 关键事实: 引擎的行情**不走**这三库

`stockdb/sync_url.txt` 写明: 「原理: **数据源 -> 同步数据 -> 你的/data**」,
源为 Cloudflare 后的 `a.123128.xyz`。实测:

- 三库 mtime 全停在 **09-03**;
- 同时 `stockdb.exe` 的 SDK(`pybao/stock_sdk.rd`)能返回 **09-21/09-22 的真实行情**
  (平安银行 09-22 收 11.71、浦发 9.04, `pre_close` 自洽);
- ⇒ **引擎走远端取数, 三库只是同步缓存**。

### 为什么"补"不划算

| 维度 | 分析 |
|---|---|
| 补的代价 | 必须先 `Stop-Service AStockStockdb` 释放 leveldb 独占锁 -> 跑更新器 -> 再启动。**代价是引擎停机** |
| 补的收益 | **0** —— 评估链路只读 h5i, 从不读这三个库(全仓检索确认: 仅在 `engine_bars_sync.py` 的文档注释里被提及) |
| 不补的风险 | **无**(引擎走远端) |

**决定: 不补。** 三库停在 09-03 是**已知状态, 不影响评估链路**。

### `data/LOCK` 的影响面(已评估)

leveldb 用 OS 独占文件锁, `stockdb.exe` 持有 `data/LOCK`。影响面**仅限**
「谁要写这三个库」—— 即 `数据更新.exe`。它不影响:

- 引擎的读(`rd.vals()`)—— 因为读的是远端;
- 本仓任何代码 —— 本仓不碰这三个库。

故无需为释放锁而停机。

---

## 3. 厂商是否停更: 证据与判断框架

### 实测证据

| 证据 | 指向 |
|---|---|
| 三库 mtime 停在 09-03(**约 20 天未写入**) | 同步链路已停 |
| `数据更新.exe` ExitCode=**0**、stdout/stderr **均 0 字节** | 「成功但什么都没做」 |
| 该进程约 **40 秒**后**静默驻留**(不退出、CPU 冻结在 10.2s、内存 29MB) | **等锁**或**等 GUI 交互**, 不是"同步失败" |
| 该进程**零网络连接** | 根本没尝试连接 |
| `log.txt` mtime 仍为 09-03, **未被写** | 更新器未进入工作阶段 |
| 主窗口标题 `数据更新`、`Responding=True` | 是 GUI 程序, 无头启动后等人操作 |

### 判断框架

**支持「厂商侧停更/授权到期」**: 20 天零写入 + 更新器零网络 + 日志未写。

**支持「本地锁/GUI 问题」**: 更新器有窗口且响应, 零输出 —— 更像卡在等待,
而非被服务端拒绝。**注意: 这一条削弱了"厂商停更"的推断**, 因为若是服务端拒绝,
通常会在 stdout/日志留痕。

> **诚实结论: 无法从本机证据区分这两者。** 要区分必须:
> ① 用管理员权限 + 图形界面跑一次更新器, 看有无交互提示(登录/续费);
> ② 查厂商文档 `stockdb/调用方式/` 确认有无**非交互式**同步接口;
> ③ 直接联系厂商确认授权状态。

### ⚠️ 一个重要的反面观察(2026-09-22 22:00 实测)

**"三库未写入"并不等于"数据没到"**: 同日引擎 SDK 已能返回 09-21/09-22 的
真实行情, 而三库仍是 09-03。故 **不能**用三库 mtime 推断"厂商是否发布" ——
它只反映**同步缓存**是否更新。

---

## 4. akshare 作为备选主源: 口径验证结果

### 验证方法

抽样 60 只在市标的(2026-09-18 由 h5i 取), 对比引擎与 akshare
`stock_zh_a_hist` 在**重叠日 09-18** 的 OHLC / 涨跌幅 / 成交量 / 成交额。

### 结果

| 维度 | 结果 |
|---|---|
| OHLC / 涨跌幅 逐值一致 | **15/16** |
| `volume` 比值(引擎/akshare) | **mean = 100.0001**, min 100.0000, max 100.0016 |
| `amount` 1:1 | **16/16** |
| 缺口日(09-21/09-22)可取 | **32/32** |

**口径换算(已确认)**:
- OHLC、`pct_chg`: **1:1**, 无需换算;
- `volume`: akshare 是**手**, 引擎是**股** ⇒ **×100**(实测比值 100.0000~100.0016);
- `amount`: **1:1**(单位: 元)。

### 那 1 例不一致: 600177 的复权口径

| | close | pre_close | pct_chg |
|---|---|---|---|
| 引擎 09-18 | 8.14 | **8.10** | **+0.49** |
| akshare(不复权) 09-18 | 8.14 | 8.30(隐含) | **−1.93** |

**引擎是自洽的**: `8.14 / 8.10 − 1 = +0.4938%` ✓, 与它自己的 `pct_chg` 相符。
而 akshare 不复权用 8.30 作前收(除权参考价)。⇒ **这是复权口径差异, 不是数据错误**。

**状态: 假设已强证据支持, 但未完成独立验证** —— akshare 在验证过程中触发限流
(`ConnectionError`, 约 80 次请求后), 未能取到 `adjust="qfq"` 对比确认。
**待办**: 限流恢复后用 `adjust="qfq"` 复核 600177 的涨跌幅是否等于引擎的 +0.49。

### 切换主源的前提(当前未满足)

1. 完成上述复权口径复核(至少抽样确认, 不能只看 1 例);
2. 确认 akshare 的**稳定性与限流行为**足以支撑每日全市场拉取;
3. 建立**双源交叉校验**再切换, 而不是直接替换。

**当前定位: `stockdb` 仍是主源**(引擎可用且数据已到 09-22);
**akshare 是已验证口径的备份源**, 随时可启用。

---

## 5. 相关登记

- `P1-STOCKDB-UPDATER-NO-OP` —— 更新器零产物(open, 需人工核实厂商侧)
- `P1-ENGINE-CALENDAR-UNIVERSE-DIVERGENCE` —— 引擎"日历"实为数据覆盖(fixed, 现象已消除)
- `P1-ENGINE-CALENDAR-ROOT` —— 根治: 日历判据不得取自引擎数据覆盖(P0 层面的哨兵修复已落地)
- `P0-H5I-BACKFILL-0921-0922` —— h5i 缺口补齐与重跑选股(fixed)

---

## 6. Baostock 口径验证与能力边界(2026-09-22 实测)

> 用户提议引入 Baostock 做历史补数。以下**逐项实测**, 并**纠正提议中的两处**。

### 6.1 口径: 与引擎**完全一致**(比 akshare 更好)

抽 **8 只标的 × 3 个交易日**(09-18/21/22), `adjustflag="3"`(不复权):

| 维度 | 结果 |
|---|---|
| OHLC **逐值一致** | **8/8 只 × 3 日, 全部一致** |
| `volume` 比值(引擎/baostock) | **min = max = mean = 1.0000** |
| `amount` | 1:1 |

⇒ **Baostock 的 volume 单位是「股」, 与引擎同口径, 不需要 ×100**
(这点与 akshare **相反** —— akshare 是「手」, 必须 ×100)。
**两源混用而不按源声明换算, 会得到差 100 倍的量且没有任何报错。**

### 6.2 ⚠️ 纠正提议: `adjustflag` 的取值

提议写「`adjustflag="2"` # 2=前复权(推荐)」。**实测三种取值的浦发 09-18 close**:

| adjustflag | close | 含义(**实测确认**) |
|---|---|---|
| `1` | **121.24** | **后复权**(价格被大幅放大) |
| `2` | 9.07 | **前复权** |
| `3` | 9.07 | **不复权** |

即 **1=后复权 / 2=前复权 / 3=不复权**(与 baostock 官方文档一致)。
**本仓对齐口径是 `3` 不复权**: 引擎 09-18 浦发 close = **9.07**, 与 flag=3 一致。
若按提议用 `2`, 在**无除权事件的区间**恰好也是 9.07(所以小样本看不出差别),
但一旦区间内有分红送转,**两者会分道扬镳** —— 这正是"小样本验证通过、上线才炸"的典型。

### 6.3 ⚠️ 纠正提议: 它是**逐 symbol** 接口, 无法按日批量

| 尝试 | 结果 |
|---|---|
| `query_all_stock(day)` | 返回 **7393 行**, 但字段只有 `code, tradeStatus, code_name` —— **不含 OHLCV** |
| 该接口是否给 `type`(区分个股/指数/ETF) | **不给**。要判断 `type` 必须**逐个**调 `query_stock_basic` |

⇒ **全市场每日取数 = 逐只 5481 次请求**。实测吞吐(**无间隔**连打 20 只):
**20/20 成功, 150 ms/只, 零限流**。推算:

| 间隔 | 全市场 5481 只 |
|---|---|
| 无间隔(**实测**) | **13.7 分钟** |
| 0.3s(实测上限 + 余量) | 27.4 分钟 |
| **1.5s(提议的限流间隔)** | **2.28 小时** |

> 好消息: 提议的 `1.5s` 比实测需要**保守得多**(实测 150ms 就能稳定跑)。
> **但即便是 13.7 分钟, 也无法承担"每日全市场"** —— 本仓收盘管道 19:10 启动、
> 窗口到 22:00, 而它还要跑选股/回测/训练; 再叠 14~27 分钟的单源取数不合理。
> 且识别哪些代码是**个股**(排除 ~1900 个 ETF/指数)还要再花 6897 次 `query_stock_basic`。

### 6.4 覆盖缺口: **北交所无数据**

`bj.430047` 等北交所代码 `query_stock_basic` 返回**空**。故 Baostock 只覆盖**沪深**。
本仓 h5i 里若有北交所标的(实测 09-22 全市场 5481 只含 `8*`/`4*` 前缀),
**Baostock 补不了**。

### 6.5 结论: 定位为「定向补数与交叉校验」, 不作每日主源

| 场景 | 推荐 | 理由 |
|---|---|---|
| **定向历史补数 / 缺口回填** | **Baostock** | 口径与引擎逐值一致、无需单位换算、限流宽松; 逐只取数对**少量标的**正合适 |
| **双源交叉校验** | **Baostock**(优于 akshare) | 口径一致 ⇒ 差异即真异常; akshare 还需先处理 ×100 与复权口径 |
| 每日全市场摄入 | **维持引擎(stockdb SDK)** | Baostock 逐只 13.7+ 分钟, 不适合每日管道 |
| 实时行情 | 引擎 / AkShare | Baostock **无实时数据** |
| 另类数据(龙虎榜/北向) | AkShare | Baostock 不覆盖 |

**已落地**: `baostock` 已登记进 `bars_ingest.SOURCE_SPECS`
(`volume_mult=1.0`, `symbol_strip_prefix=True` —— 它用 `sh.600000` 形式,
不剥前缀会**静默匹配不上**任何按裸代码查的东西), 归一化契约已由
`tests/test_bars_ingest.py::TestEveryRegisteredSourceSatisfiesTheContract` 对**每个源**自动验一遍。

### 6.6 ⭐ 决定性验证: 在**含除权事件**的区间上确认 `adjustflag=3`

> 用户点出的关键: **无除权事件的区间上, 前复权(2)与不复权(3)恰好相同**,
> 所以用近端区间"验证通过"**证明不了什么**。

**找除权事件的方法**: 引擎的 `pre_close` 是**除权调整后**的, 故
`pre_close != 前一交易日 close` 即该日发生除权。扫描得:
**600177 @ 2026-09-18**(前收 **8.30** → `pre_close` **8.10**)。

**结果(同一标的、同一区间, 三种 adjustflag vs 引擎)**:

| adjustflag | OHLC 与引擎一致 | 09-17 close | 判定 |
|---|---|---|---|
| `1` 后复权 | **0/3** | 472.78 | ✗ |
| `2` 前复权 | **2/3** | **8.1000** ← 被复权缩放 | ✗ |
| **`3` 不复权** | **3/3** | **8.3000** | **★ 唯一正确** |

**⇒ `adjustflag='3'` 是唯一与本仓口径一致的取值**; 前复权在除权日会露出马脚
(09-17 的历史价被按除权比例缩成 8.10)。这正是"只有除权区间才能暴露口径差异"的实证。

### 6.7 ⭐ 附带发现: 必须取源的 `pctChg`, **不能自算涨跌幅**

在同一除权日(600177 @ 09-18):

| 来源 | 值 | 说明 |
|---|---|---|
| **自算** `(8.14/8.30-1)` | **−1.93%** | 用**未调整**前收 ⇒ **错** |
| **引擎** `pct_chg` | **+0.49%** | 基于**除权调整后** `pre_close=8.10` |
| **baostock** `pctChg` | **+0.4938%** | 与引擎**一致**(它也返回 `preclose=8.10`) |

⇒ 两源在这一天**其实是一致的**, 但**只有取源字段**才对。
**若我们只取 OHLC 而自算涨跌幅, 会在每个除权日静默产出错误因子** ——
这是"少取一个字段就静默算错"的实例。

**故 baostock 的源规格用全字段集**, 显式映射
`pctChg -> change_pct`、`turn -> turnover`(实测 `turn=0.5328` 对引擎 `turnover=0.53`);
`preclose` 一并取出以备核对。已由测试锁住(自算值 −1.93 会被判为不一致)。

### 6.8 阶段二骨架已落地

| 模块 | 作用 |
|---|---|
| `src/baostock_adapter.py` | 三契约: `RateLimiter`(200ms→1.5s 退避) / `classify_targets`(用 symbols.parquet) / `classify_outcome`(`{appended, partial, unfillable_gap, failed}`) |
| `src/cross_validate.py` | 双源逐值校验, 判据**写死预期**(`volume_ratio`: baostock/引擎=**1.0**, akshare=**100.0**) |

**真实双源校验结果(含除权日)**:
```
[cross_validate baostock] OK  比对 15 组  volume比值=1.0000 (预期 1.0)  失败判据=[]
  [OK] OHLC 逐值相等(容差 0.0001) -- 15/15 组一致
  [OK] change_pct 一致(容差 0.02) -- 一致
  [OK] volume 比值 == 预期 1 -- 实测 1.0000
  [OK] volume 逐值相近(容差 100 股) -- 股 vs 股, 应逐值相等
```

**契约二实测**: 用 h5i `symbols.parquet`(**不调** `query_stock_basic`):
可取 **5212**(sh 2315 + sz 2897), **北交所 339 只显式标为不可得**。

### 6.9 实现中实测踩到并修掉的两个 bug(都有回归)

1. **空数据触发了退避**: 退避计数把所有失败都算上 ⇒ 某批标的全部停牌/退市
   (空数据, 完全正常)会把间隔从 200ms 抬到 1.5s, **把整批从 13.7 分钟拖到 2.28 小时**。
   已改为**只对限流特征退避**。
2. **`920xxx` 被判成沪市**: 北交所代码是 `920xxx`, 而 `9` 开头在沪市是 B 股 ——
   首版先判 `9* -> sh`, 使 `920000` 得到 `sh.920000`。已把 `920` 判定**前置**。

### 6.10 ⚠️ 回填完成后必须**同时**验证两件事(2026-09-25)

这一节是为了防止一个具体的误判。回填(把 Baostock 的日线补进 h5i)**只动 h5i 的水位**,
**不动厂商引擎** —— 故回填后**必须分别验证两个独立的事实**, 少验一个就会得出错误结论:

| # | 要验的事 | 怎么看 | 期望 |
|---|---|---|---|
| ① | **h5i 真的补进去了**(下游受益) | `h5i.daily_bars` 水位 / `bars_on_day(d)` / `symbols_on(d)` | 水位前进到目标日; 该日行数 ≈ 5000+ |
| ② | **引擎探针**仍然停在旧日(选股口径未变) | `engine_bars_sync.freshness()` 的 `engine_day` / `lag_trading_days` | **与回填前相同** —— 这不代表失败 |

**这两个期望是相反的, 而它们同时成立才是正确结果。** 只验 ① 会以为"选股也能用新数据了";
只验 ② 会以为"回填没生效"。

**2026-09-25 实测(可作为对照基准)**:

```
① h5i.daily_bars 水位: 2026-09-22 -> 2026-09-24   (距今 3 天 -> 1 天)
   bars_on_day('2026-09-23') = 5200 行   symbols_on('2026-09-24') = 5200 只
② freshness: engine_day=20260922, expected_day=20260924, lag_trading_days=2
   —— 与回填前**完全一致**(引擎探针读的是厂商引擎, 不是 h5i)
```

⇒ 下游拿得到完整序列, 而选股口径没变。

**为什么这个区分必须写进文档**: 回填后**最容易发生的误读**是 ——
下次盘后看到 `target_plan.data_lag_days` 仍是 3, 便判断"回填失败了"。
实际它**本来就不会改善**: `section_as_of` 取自引擎探针, 只受厂商发布影响。
**回填的受益方是读 h5i 的下游(回测 / 因子 / IC), 不是选股。**

**留痕已记下这一点**: `data/backfill_provenance.jsonl` 的每条记录含
`engine_probe_unchanged: true` 与 `affects_selection: false`,
并写明"回填不改变选股口径" —— 任何看到 `data_lag_days` 异常的人都能据此追溯到原因,
不必重新推导一遍。

### 6.11 ⏳ **自动回填端到端: 尚未验证** (2026-09-25 登记 —— 请勿当成已验证)

> **判定通过 ≠ 自动回填已验证。**

这一节存在的唯一目的是**阻止一次具体的误读**: 看到"触发判据有 35 条守卫、
全部通过"就以为"自动回填已经能用了"。**两者不是一回事。**

## 已验证到哪一步(逐项, 不夸大)

| 环节 | 状态 | 证据 |
|---|---|---|
| 触发**判定**逻辑 | ✅ 已验证 | `backfill_trigger.decide` 纯函数, 35 例守卫; 含用户的验收条件(engine=09-22 / h5i=09-24 ⇒ 不触发) |
| 回填**取数+写入**(手工) | ✅ 已验证 | 2026-09-25 实跑 `--write`: 5200 行 × 2 天, 水位 09-22 → 09-24 |
| 写入路径(隔离库) | ✅ 已验证 | 生产同款 schema, `ts` 非空、区间幂等、不动相邻日 |
| **run_daily 内的自动触发 → 取数 → 写入** | ❌ **未实跑过** | 从未在真实缺口下开启 `BACKFILL_ENABLED=1` 跑过一轮 |
| 「两件事同时验证」的自动结论 | ⚠️ 代码就位、**未在真实回填中产出过** | `verify_two_things()` 有守卫, 但只在 mono 测试里跑过 |

## 为什么还没验证

`BACKFILL_ENABLED` **默认关闭**, 且开启需要满足全部条件:

1. **A 且 B 同时成立**(引擎缺口 **且** h5i 缺口) —— 2026-09-25 手工回填之后
   **B 已是假**(h5i 已到 09-24), 故**当前状态下不会触发**, 也就没法验证;
2. 门禁不是 HALT 状态。

⇒ 要等**下一次真实缺口**(如 09-28 厂商仍未恢复, h5i 又缺 09-25/09-28)。

## 怎么验证(下次执行时的检查单)

```powershell
# 前提: 确认 A 且 B 成立(不应触发时不要开)
python src/backfill_trigger.py --evaluate

# 开启并跑一轮收盘管道; 观察 steps.backfill_trigger
$env:BACKFILL_ENABLED=1
python src/run_daily.py            # 或等守护自动触发

# 验收三件:
#   ① 自动触发(step 里 action=trigger 且有 run 段)
#   ② 补数成功(run.written 非空)
#   ③ 留痕含 engine_probe_unchanged=true(data/backfill_provenance.jsonl)
```

**另需核对**: `steps.backfill_trigger.run.verify` 里 ① `h5i_advanced=true`
且 ② `engine_unchanged=true` —— 两者**同时为真**才算符合预期(见 §6.10)。

## 验证通过后要做的事

把本节的标题从 **⏳ 尚未验证** 改为 **✅ 已验证**, 并写上**日期与实测数字**
(哪一天触发、补了几个交易日、多少行)。**在那之前, 任何"自动回填已就绪"的说法都不成立。**

### 6.12 `BACKFILL_ENABLED=1` 的**时间窗口**: 09-28 前安全, 09-28 起可能触发

> 用户 2026-09-25 要求标注这个窗口。**它与"能不能验证"是同一件事的两面。**

## 结论

| 时间 | 打开 `BACKFILL_ENABLED=1` 会怎样 | 依据 |
|---|---|---|
| **现在 ~ 09-27(周日)** | ✅ **安全, 不会触发** | B 为假(h5i 已 09-24 = expected) ⇒ `no_gap` |
| **09-28(周一)19:10 起** | ⚠️ **可能触发** | `expected_day` 前进到 09-28, 而 h5i 水位停在 09-24 ⇒ B 变真 |

## 为什么 09-28 前是安全的(判据链, 已实测)

`expected_day` = **最后一个已收盘的交易日**(取自交易日历, 不是"今天"):

| 时点 | `expected_day` | h5i 水位 | B = 水位 < expected | 结果 |
|---|---|---|---|---|
| 09-25(周五, 已收盘) | 09-24 | 09-24 | **假** | `no_gap` |
| 09-26 / 09-27(周末) | 09-24 | 09-24 | **假** | `no_gap` |
| **09-28(周一)收盘后** | **09-28** | 09-24 | **真** | 取决于 A |

**实测**(2026-09-25 生产入口 `python src/backfill_trigger.py --evaluate`):

```
默认:               action=disabled
BACKFILL_ENABLED=1: action=no_gap  criteria={A: true, B: false}
                    reason: A 真但 B 假 —— 引擎停在 20260922 而 h5i 已到 20260924
                            (已经补过了, 再补是重复动作)
```

即**现在打开也不会补** —— 因为存储侧已经补过了。

## 09-28 那一刻两种走向(取决于厂商)

| 厂商 | 收盘管道内的顺序 | A | B | 结果 |
|---|---|---|---|---|
| **恢复发布** | `engine_bars_sync` 先跑(19:10 步 1.05) ⇒ h5i 水位前进到 09-28 ⇒ 此时 B **已假** | 假 | 假 | ✅ **不触发** —— 正是"A 且 B"想要的行为 |
| **仍未发布** | 引擎仍 09-22, h5i 仍 09-24 ⇒ A 真、B 真 | 真 | 真 | ⚠️ **第一次真实触发回填** |

**注意顺序很关键**: `engine_bars_sync` 在 `backfill_trigger` **之前**执行,
所以"厂商恢复"与"厂商没恢复"会被自然地区分开 —— 恢复时 B 当场变假, 不必靠人判断。

## 开启方式(有坑, 必读)

**`$env:BACKFILL_ENABLED=1` 对守护自动拉起的那次 `run_daily` 无效** ——
`run_daily` 由守护进程 `Popen` 拉起且**未传 `env=`**, 继承的是**守护进程的环境**。

故有两条路:

```jsonc
// ① 开关文件(推荐 —— 守护拉起的那次也能看到)
//    data/backfill_switch.json   (data/ 已 gitignore, 属机器本地状态)
{"enabled": true, "lookback_days": 10}
```

```powershell
# ② 环境变量(只对手工跑的那次有效)
$env:BACKFILL_ENABLED=1 ; python src/run_daily.py
```

**取值优先级**(与 `factor_gate` 同一约定): **环境变量 > 开关文件 > 默认值(关)**。

**⚠️ 该文件必须用 `utf-8-sig` 读**: PowerShell 的 `Out-File -Encoding UTF8` /
`Set-Content -Encoding UTF8` **会写 BOM**, 普通 `utf-8` 读会抛
`Unexpected UTF-8 BOM` ⇒ 被吞掉 ⇒ **文件明明写着 `enabled: true` 却读成"没配"**,
开关静默保持关闭(实测踩到, 已修)。与 `metrics_server` 的 `fusion_health.json` 同一个坑。

**坏文件一律当"没配"(= 关)**: 宁可少补一次(人工可补), 不可误补一次(不可逆)。

## 打开的**前提**(否则不要开)

1. `python src/backfill_trigger.py --evaluate` 显示 `action ∈ {no_gap, disabled}` 之外
   才是"会补"; **想只是预备着, 开着也无害**(当前就是这状态);
2. 确认门禁不是 HALT;
3. **知道它会写生产行情库**(不可逆)。

### 6.13 09-28 观察清单(19:10 收盘管道之后)

> **状态: 开关已按 09-28 预案开启** —— 2026-09-25 已创建
> `data/backfill_switch.json`, 内容 `{"enabled": true}`, 用 **ascii 编码(无 BOM)** 写入。
> 实测该文件已生效: 用**生产解释器**跑 `is_enabled()` 返回 `True`
> (即守护拉起的那次 `run_daily` 会看到它 —— 这一条必须用生产解释器验,
> 因为在 shell 里设 `$env:` 对守护拉起的进程**无效**, 见 §6.12)。

## 检查顺序(按这个顺序看, 不要跳)

```text
□ 1. 厂商是否恢复?
     看 data/health/state.json 的 observed.engine_day 与 lag_trading_days
     或 python src/engine_bars_sync.py --probe
```

**若恢复**(引擎追平):

```text
□ 2a. EngineDataLag 自动 resolve(Alertmanager 里该告警消失)
□ 2b. steps.backfill_trigger 的 action = no_gap
□ 2c. 结论: **安全不触发** ✅ —— 这正是「A 且 B」想要的行为
        (engine_bars_sync 在 trigger 之前跑, 水位当场前进 ⇒ B 变假)
```

**若未恢复**(引擎仍停旧日):

```text
□ 2a. steps.backfill_trigger 的 action = trigger
□ 2b. 补数 09-25 / 09-28
□ 2c. 验证**两件事同时成立**(§6.10):
        ① steps.backfill_trigger.run.verify.h5i_advanced   = true
        ② steps.backfill_trigger.run.verify.engine_unchanged = true
□ 2d. 确认 unfillable_symbols 含 **339** 只北交所(§6.11 第 3 项 / 留痕字段)
□ 2e. 通过后: 把 **§6.11 标题改为 ✅ 已验证** + 写上实测数字
        (哪一天触发 / 补了几个交易日 / 多少行)
```

## 关于「长期开启」

**首次自动触发验证成功后, 建议保持开启**:

1. **触发条件严格** —— A 且 B, 且门禁非 HALT(见 §6.12);
2. 它正是「数据及时更新」的**冗余机制** —— 主源(厂商引擎)不发布时, 系统自己兜一层;
3. **关闭它等于回到「靠人记得手动补」** —— 那正是本仓一贯要消除的东西
   (靠注意力而非对照)。

**但若首次触发出现任何异常**:

```text
□ 立即把 data/backfill_switch.json 的 enabled 改为 false
□ 记录异常(日期 / action / 报错 / 留痕内容)
□ 修复后再开 —— 期间靠人工补
```

**判据**: 异常时**先关掉再排查** —— 因为开着但行为不明, 会在下一次收盘管道里
**再次写生产行情库**, 把现场覆盖掉。**先止损, 再定位。**


